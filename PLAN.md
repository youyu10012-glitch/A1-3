# TripWhisper 서비스 기획서

## 1. 서비스 개요

- **서비스명:** TripWhisper
- **한 줄 소개:** 여행자의 취향과 맥락을 읽고 국내의 다음 장면을 제안하는 AI 여행지 큐레이터
- **목적:** 검색 결과를 나열하는 대신, 사용자의 현재 여행 목적과 동행 상황에 맞는 한 곳의 이유 있는 추천을 제공한다.
- **핵심 가치:** 입력은 간단하게, 결과는 구체적인 장면과 실행 가능한 팁으로 제공한다.

## 2. 타겟 사용자

- 주말에 짧은 국내 여행을 계획하는 20~40대 직장인
- 검색을 많이 하기보다 취향에 맞는 후보를 빠르게 좁히고 싶은 사람
- 혼자, 연인, 친구 등 동행자에 따라 여행의 속도를 달리하고 싶은 사람

## 3. 페이지 및 이동 구성

1. **Hero / 홈:** 서비스의 정체성과 핵심 CTA를 보여주고 추천기로 이동한다.
2. **서비스 소개:** TripWhisper가 취향, 맥락, 장면을 연결하는 방식을 설명한다.
3. **AI 추천기:** 여행 스타일, 일정, 동행자를 입력해 맞춤 여행지를 요청한다.
4. **FAQ:** 추천 기준, 정보 저장 여부, 결과의 성격을 안내한다.
5. **Footer:** 서비스 식별 정보와 마무리 메시지를 제공한다.

상단 네비게이션은 앵커 링크 방식으로 각 섹션에 즉시 이동한다. 모든 섹션은 단일 페이지에 배치해 모바일에서도 스크롤 흐름이 자연스럽게 이어진다.

## 4. AI 기능 설계

### 입력

- 여행 스타일: 자연, 미식, 카페, 액티비티 등 사용자가 원하는 분위기나 활동
- 일정: 당일치기, 2박 3일, 이번 주말 등 이용 가능한 시간
- 동행자: 혼자, 연인, 가족, 친구 등 여행 구성

### 처리

브라우저가 `/api/recommend`에 JSON POST 요청을 보낸다. Vercel Python Serverless Function은 `GEMINI_API_KEY`가 있으면 Google Gemini에 구조화된 JSON 응답을 요청하고, 키가 없거나 외부 API 호출이 실패하면 목업 추천을 반환한다. 따라서 키를 등록하기 전에도 화면과 요청 흐름을 테스트할 수 있다.

### 출력

- 추천 여행지 제목 및 지역
- 추천 장소에서 경험할 여행 장면
- 입력 맥락과 연결된 추천 이유
- 추천 시기 및 실용적인 작은 팁

### 실패 처리 기준

- **빈 입력:** 브라우저에서 요청 전에 `필수값을 입력하세요` 안내를 표시한다.
- **잘못된 요청(400):** 서버가 입력 검증 메시지를 JSON으로 반환한다.
- **AI/API 오류(500 또는 외부 호출 예외):** 사용자에게 잠시 후 다시 시도하라는 메시지를 보여주고, 서버 내부적으로는 목업 결과로 안전하게 대체한다.
- **10초 초과:** 브라우저의 `AbortController`가 요청을 중단하고 응답 지연 안내를 보여준다.
- **쿼터 및 비용:** 추천 버튼의 단일 요청 구조를 사용하고, 키가 없는 환경에서는 목업으로 동작시켜 개발 중 불필요한 API 호출을 줄인다.

## 5. 반응형 및 접근성 기준

- 480px 이하 모바일, 800px 이하 태블릿, 데스크톱 레이아웃을 분기한다.
- 입력 요소에는 명시적인 label을 사용하고 결과/오류 메시지는 `aria-live`로 전달한다.
- 키보드로 앵커, 폼, FAQ를 조작할 수 있도록 기본 HTML 요소를 활용한다.

## 6. 검증 시나리오

1. 스타일, 일정, 동행자를 모두 입력하고 목업 추천이 결과 패널에 나타나는지 확인한다.
2. 하나라도 비워 제출해 `필수값을 입력하세요` 메시지를 확인한다.
3. 모바일 폭에서 네비게이션, 추천 폼, 결과가 가로로 넘치지 않는지 확인한다.
4. `GEMINI_API_KEY`를 설정한 배포 환경에서 Gemini 응답이 동일한 JSON 필드로 표시되는지 확인한다.
